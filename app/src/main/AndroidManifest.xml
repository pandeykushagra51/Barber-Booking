<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mydream">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyDream">
        <activity android:name=".BookItemActivity"></activity>
        <activity android:name=".AddItemActivity" />
        <activity android:name=".SellerDetailActivity" />
        <activity android:name=".SellerLoginActivity" />
        <activity android:name=".CustomerDetailActivity" />
        <activity android:name=".SellerFormActivity" />
        <activity android:name=".ItemMainPageActivity" />
        <activity
            android:name=".CustomerLoginPageActivity"
            android:label="@string/title_activity_customer_login_page"
            android:theme="@style/Theme.MyDream.NoActionBar" />
        <activity android:name=".CustomerFormActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>